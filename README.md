# Project Amethyst
## Building from source
I'm assuming you already have a terminal open in the project folder and Node.js installed.
1. Run `npm i` to install dependencies
2. To build, run `npm run build`
## Documentation
Go [here](docs/docs.md).